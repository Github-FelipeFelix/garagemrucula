# Memory Index — Garagem Rúcula

## ➤ ONDE PARAMOS — ler ISTO primeiro · atualizar por último (att. 02/07/2026 — feedback do PWA: 7 fixes [scroll das miniaturas / lightbox / selo / seta do card / ÍCONE maior / login mobile] + AUTONOMIA no admin [aba "Textos do site"]; 5 carros reais no ar)
> Continuidade entre os 2 PCs via **GitHub**. Início: hook roda `git pull`. Fim: `git push`. Canônico também no `CLAUDE.md` da raiz.

**🟢 SITE NO AR (domínio próprio): https://www.garagemrucula.com.br** (apex → 308 pro www; `garagemrucula.vercel.app` também responde). v1 + cosméticos "UAU" + fotos HD + **lightbox c/ zoom** + **carrossel no card** + filtros mobile + OG image + **login com Google** + **admin de fotos usável no celular** + **acesso ao admin pelo celular** + **galeria com setas/swipe** + **troca de foto INSTANTÂNEA** (pré-carrega vizinhas) + **fix "super zoom" no celular** + **selos padronizados** + **marca SEM acento ("Rucula")** + **editor de textos do site no admin** (Sobre/hero) — tudo no ar. Deploy automático a cada `git push origin main`.

**AMBIENTE:** o projeto vive em **`C:\dev\garagemrucula`** (disco local — NÃO no Google Drive; o `npm install` corrompe lá com EBADF). GitHub `github.com/Github-FelipeFelix/garagemrucula` (**PÚBLICO**) é a fonte da verdade. **Sempre abrir o Claude em `C:\dev\garagemrucula`.** Hook `SessionStart` → `sessao-inicio.ps1` faz `git pull`. **PC novo:** `git clone` → `git config user.email felipeherrera.contato@gmail.com` → copiar `.env.local` (guardado na pasta antiga do Drive) → `npm install` (Node ideal 22+; no 20 dá só warning).

**Projeto:** vitrine de carros do primo do Felipe (antigos/importados/modificados — turbo, rebaixado). Cliente vê o disponível → vai pro WhatsApp (+55 19 97416-5880, msg pronta) ou Instagram @garagem_rucula. `/admin` mobile-first (o primo é leigo, usa do celular em eventos).

## 🔜 PENDÊNCIAS — COMEÇAR A PRÓXIMA SESSÃO POR AQUI
> ⚠️ Felipe pediu (02/07): estas ficam como pendência e **NÃO implementar por ora** (só se ele pedir). 2FA saiu da lista (ele não quer como pendência). Teste logado do admin: FEITO com a conta real.
1. 💡 **E-mail/notificação de novo lead** (Resend free): hoje o primo só vê interesse abrindo o admin. Alto valor — responder lead quente rápido.
2. 💡 **HEIC do iPhone no PhotoUploader** (validar/converter; irrelevante se o primo usa Android).
3. 💡 **Rate-limit distribuído (Upstash free)** em `/api/lead`, mesma assinatura de `rateLimit()`.
4. 💡 **Autonomia+ (só se pedir):** WhatsApp/Instagram no editor "Textos do site" (COM validação — é o CTA de venda) e/ou ícones dos diferenciais editáveis. Hoje o editor cobre hero + Sobre (parágrafos) + diferenciais (texto).
5. 🧹 **Dívida cosmética:** 5 lint errors preexistentes (`react-hooks/set-state-in-effect`, não quebram build); zoom por scroll no Lightbox (desktop); limpar assets antigos do cache do SW.
6. 🔎 *(só se repetir)* Login Google "fresh" — provável já resolvido pelo fix do OAuth mobile (redirect manual); se repetir, ver logs da Vercel.

**✅ Resolvido em 02/07 (feedback do PWA + autonomia, esta sessão):** 7 itens do Felipe — miniaturas rolam SÓ a tira (não a página; fim do "tela descendo") + lightbox segue a foto + contorno não corta + seta do card com hit-area maior (`before:-inset-2`) + ícone do PWA bem maior + login mobile (OAuth redireciona / e-mail entra em ~1,5s) + **AUTONOMIA: aba "Textos do site" no admin** (editar hero/Sobre/diferenciais; `site_settings` jsonb, migração 0002 rodada pelo Felipe; E2E provado). Confirmado: múltiplos destaques JÁ funcionam. Ver 1º item de "JÁ FEITO".
**✅ Resolvido em 02/07 (FASE DE TESTES, Fable 5):** revisão geral completa — 20/20 páginas (desktop+iPhone `isMobile`), 27/27 interações, 10/10 segurança, produção 100% — e **4 fixes no ar**: (1) **CRÍTICO** duplicar/apagar carro compartilhava arquivos no Storage (apagar um matava as fotos do outro; agora duplicate COPIA os arquivos e delete/patch usam `removeMediaSafely`); (2) OG image com foto DEITADA (EXIF; agora passa pelo `/_next/image`) + "RÚCULA" all-caps sem acento; (3) retry transitório em `getCars`/`getCarBySlug`; (4) sitemap revalida ao salvar carro. Ver 1º item de "JÁ FEITO".
**✅ Resolvido em 02/07 (tarde):** setas+swipe na galeria, FIM do delay ao trocar foto (pré-carrega vizinhas), fix "super zoom" no celular (`grid-cols-1` mobile-first), miniatura ativa destacada, selos sólidos/padronizados + z-index dos filtros (`isolate`), rodapé centralizado e **marca SEM acento ("Rucula")** no site/admin.
**✅ Resolvido em 02/07 (mais cedo):** acesso ao admin pelo celular facilitado (atalho no app + link no rodapé + guia) e **auditoria de segurança do painel** (4 camadas OK; recomendação de 2FA pro Felipe). Ver "JÁ FEITO".
**✅ Resolvido em 02/07 (sessão no PC 2):** bug reportado pelo Felipe — pelo celular não dava pra trocar a foto de capa nem remover foto no admin. Ver "JÁ FEITO".
**✅ Resolvido em 01/07:** veredito visual dos cosméticos, domínio no ar, bug de SEO (localhost), rate-limit, fotos HD, lightbox+zoom, carrossel no card, OG image, toast, cursor pointer, **login com Google** — tudo em "JÁ FEITO".

## ✅ JÁ FEITO
- **📱 Feedback do PWA + AUTONOMIA do admin (02/07 noite, commits `5db76bd`/`7874307`/`9c6f4ab`/`73e8d4c`/`7a08d75`):** 7 pontos que o Felipe sentiu usando o PWA. (1) **Trocar foto no anúncio fazia a TELA DESCER** — o `scrollIntoView(block:nearest)` da `CarGallery` rolava a página; agora `scrollBy` só no container (QA: scrollY 0→0). (2) **Lightbox** agora a tira SEGUE a foto ativa (mesmo scroll-container). (3) **Contorno da miniatura ativa cortava no topo** → `py-1.5` na tira. (4) **Seta do card**: near-miss entrava no anúncio → `before:-inset-2` amplia a área clicável ~50px sem inchar o visual. (5) **Ícone do PWA** bem maior (sharp: trim + 94%/78% maskable; ⚠️ reinstalar o app pra ver). (6) **Login mobile**: OAuth esmaecido não redirecionava → `skipBrowserRedirect:true`+`window.location.href` (QA: vai pro Google); e-mail "carregando" eterno → `window.location.assign` (QA conta REAL: ~1,5s). (7) **AUTONOMIA — aba "Textos do site" (`/admin/site`)**: primo edita subtítulo do hero + 2 parágrafos do Sobre + 3 diferenciais sem código; `site_settings` (jsonb 1 linha; migração 0002 rodada pelo Felipe; RLS: anon lê, service_role escreve); páginas públicas leem com `getSiteSettings()` (degrada pros defaults sem a tabela) e revalidam ao salvar; **E2E provado** (editar→site→restaurei, banco limpo). WhatsApp/Instagram FORA de propósito (CTA de venda; risco pra leigo). Confirmado ao Felipe: **múltiplos destaques JÁ funcionam** (checkbox por carro, até 6). QA: build ok, 20/20 páginas, login real, E2E do editor. Aprendizados #42–44.
- **🧪 REVISÃO GERAL / FASE DE TESTES (02/07, Fable 5, commits `d7d15c8`/`fd08b69`/`4ed134d`/`2fe8527`):** auditoria estática de todo o src (RLS, sanitização, proxy, SW, PWA, SEO — OK) + QA runtime build de prod: **20/20 páginas** desktop+iPhone13 `isMobile` (viewport/overflow/console/requests), **27/27 interações** (galeria/lightbox/filtros/carrossel/share/interesse [lead interceptado, nada gravado]/menu/back-to-top), **10/10 segurança** (401 APIs; redirect+`next` páginas), produção 100%. **Fixes:** (1) **CRÍTICO** `storage-media.ts` — duplicate copiava só a REFERÊNCIA das fotos (mesmos paths) e delete removia do bucket → apagar original/cópia matava as fotos do sobrevivente; agora duplicate COPIA os arquivos (`storage.copy`), DELETE remove só paths que nenhum outro carro usa, PATCH limpa órfãos da edição; mecânica validada no Storage real (8/8), 73 mídias dos 5 carros OK. (2) OG image: foto deitada (EXIF que o Satori não aplica → capa via `/_next/image`) + "GARAGEM RUCULA" sem acento (all-caps tinha escapado do replace case-sensitive). (3) `withRetry` nas leituras públicas (pendência #4 fechada). (4) sitemap em `REVALIDATE_PATHS` (carro novo indexa na hora; provado: 5/5 no sitemap pós-deploy). **Não-fix:** 5 lint errors preexistentes cosméticos (`react-hooks/set-state-in-effect`); Rebobina/EnergyDex sem código acessível neste PC (12 regras herdadas TODAS confirmadas aplicadas); login real do admin não testado (senha não versionada, Felipe AFK — proteções 100% validadas por fora). Aprendizados #38–41.
- **🖼️ Galeria + "super zoom" + delay + selos + marca sem acento (02/07 tarde/noite, commits `215ddc3`/`e0b2e3b`/`53520cc`/`d052d4c`):** **Setas + swipe** na foto principal da página do carro (`CarGallery.tsx`) + contador "N/M"; **miniatura ativa destacada** (border-2 + ring verde, inativas escurecidas, rola pra dentro da tira). **FIM do delay ao trocar foto** (era ~1,8s em 4G): pré-carrega a vizinha — janela `[anterior,atual,próxima]` empilhada c/ opacity + `loading="eager"` na galeria; nos cards da home dispara no 1º hover/toque (não pesa o load). **Fix "super zoom" no celular (o que o QR abria):** grids que só definiam colunas em `sm:`/`lg:` viravam grid sem coluna na base → coluna implícita `auto` esticava até o `max-content` (~1488px) → layout viewport do celular estourava pra 1504px. Fix: **`grid-cols-1` na base** (mobile-first) nos grids. PROVADO com iPhone emulado (`isMobile:true`): `innerWidth` 1504→390. Só afetava mobile REAL (desktop/QA sem `isMobile` escondem — aprendizado #33). **Selos:** fundo SÓLIDO (antes `/15` sumia); "Disponível"/"★ destaque" pílulas IDÊNTICAS, sem "bordinha preta" (Tailwind v4 `border` herda `currentColor` — aprendizado #35); "Vendido" mantém borda própria. **Selos não passam mais por cima dos filtros** (`isolate` no `CarCard` — aprendizado #36). **Rodapé** centralizado no mobile + logo centralizado com o texto no desktop. **MARCA SEM ACENTO "Rucula"** (com R, só o acento do "u" saiu) em 34 lugares de 12 arquivos do app (decisão do Felipe 02/07; os `.md` internos ficam com "Rúcula" de propósito). QA: build ok + iPhone emulado no domínio REAL (0 erro). Contexto: Felipe apagou o `fusca-teste` no meio (gerou 404 transitório, o `notFound()` agiu certo); hoje 3 carros reais no ar; o "(cópia)" é do botão Duplicar do admin (ação humana), não de código; **"fotos repetidas" NÃO é bug** (arquivos distintos, hashes diferentes — são fotos parecidas do mesmo lugar). Aprendizados #33–37.
- **📱 Acesso ao admin pelo celular + auditoria de segurança (02/07, commit `42d1cda`):** o admin é WEB — roda em qualquer aparelho; a trava é **login + whitelist** (`lib/admins.ts`), não o dispositivo (logar no PC não loga no celular; é só logar no celular, Google = 1 toque). Faltava FACILIDADE de chegar nele (não havia link pro `/admin` em lugar nenhum do site). Feito: **shortcut "Painel"** no `manifest.ts` (segurar o ícone do app instalado → `/admin`), **link discreto "Painel"** no rodapé (`Footer.tsx`, `text-muted/70`), e passo-a-passo Android/iPhone ("adicionar à tela inicial") no `COMO-USAR-O-ADMIN.md`. **Auditoria de segurança (4 camadas, todas OK):** whitelist de 2 e-mails → `proxy.ts` (`getUser()` revalida a sessão) → cada rota `/api/admin/*` re-checa `getAdminUser()` → RLS (o anon key, público, só lê carros/insere lead/bump view; NÃO lê `leads`/`car_sales` nem escreve carro; service role é `server-only`). O link no rodapé NÃO enfraquece (segurança ≠ esconder URL; estranho só vê o login). **QA REAL** Playwright com toque (desktop+mobile+prod): link → `/admin/login`, manifest serve o shortcut, **0 erro** (o único 404, `/_vercel/insights/script.js`, é do Vercel Analytics — só 404 em localhost, 200 em prod). Aprendizado #30 em `aprendizados_rebobina.md`. ⚠️ **Pendente pro Felipe: 2FA na conta Google `garagemrucula@gmail.com`** (a segurança agora se resume à conta de e-mail).
- **📱 Fix admin mobile — trocar capa/remover foto (02/07):** trocar a capa só existia via DRAG pra 1ª posição por alça de ~20px (impraticável no touch; os listeners do dnd-kit ficam SÓ na alça, tocar na foto não faz nada) e o X de remover tinha ~20px de alvo. `PhotoUploader.tsx`: **botão "⭐ capa"** em cada foto (toque → `arrayMove(i→0)`, vira capa sem drag), X e alça com 32×32px, selo "capa" com estrela; X do `VideoUploader` maior; texto de ajuda + `COMO-USAR-O-ADMIN.md` atualizados. **QA com toque REAL** (Playwright `hasTouch`+`tap()`, viewport 390): trocar capa ✓, remover ✓, 0 erros de console. Drag pela alça segue como opção. Aprendizados #28–29 em `aprendizados_rebobina.md` (alvo ≥32px + ação explícita em vez de drag; bypass do daemon órfão do dev-browser via Playwright direto de `~/.dev-browser/node_modules`).
- **🔐 Login com Google (OAuth) — FUNCIONANDO (commits `0fe4fe8`, `0bee878`):** botão "Entrar com Google" no `/admin/login` via `supabase.auth.signInWithOAuth` (mantém e-mail/senha como alternativa); rota `src/app/auth/callback/route.ts` faz `exchangeCodeForSession` (PKCE), valida `next` interno (anti open-redirect), respeita `x-forwarded-host` e loga o motivo de falhas. **Porteiro = `lib/admins.ts`** — só os 2 e-mails entram mesmo via Google (não-admin cai na home pelo `proxy.ts`). Setup do Felipe FEITO: Google Cloud (OAuth Client Web, redirect `https://lryzyydzjodywvzhiumx.supabase.co/auth/v1/callback`, app **publicado**) + Supabase (provider Google ON). Guia mastigado: `COMO-CONFIGURAR-LOGIN-GOOGLE.md`. ⚠️ Google bloqueia login em browser automatizado → validei até a tela do Google (login completo é manual). Felipe relatou 1 falha num login "fresh" (Chrome sem conta), funcionou logado — provável cookie PKCE perdido no fluxo longo.
- **✨ Leva 2 de melhorias:** **zoom no Lightbox** (`Lightbox.tsx` — toque duplo / pinça 2 dedos / arrastar-pan; swipe de navegação desliga quando ampliado; reseta ao trocar de foto); **carrossel no card** (`CarCardMedia.tsx` — setas [hover no desktop / sempre no mobile] + swipe + bolinhas c/ fundo; troca foto SEM abrir o anúncio; o `CarCard` deixou de ser `<Link>` único → foto e texto são links irmãos pro mesmo destino [evita `<button>` dentro de `<a>`], badges com `pointer-events-none`); **OG image por carro** (`src/app/(site)/carros/[slug]/opengraph-image.tsx` via next/og — foto+nome+preço+tags; removida a og:image estática do `generateMetadata`); **toast** no admin (`components/admin/Toast.tsx`, no `CarForm` ao salvar/duplicar/apagar); **cursor pointer** global em `button`/`select`/`[role=button]`/`summary` + `not-allowed` nos disabled (`globals.css`). ⚠️ O hydration mismatch que apareceu no carrossel era só dessincronia do hot-reload do dev — **zero no build de produção** (confirmado).
- **🌐 Domínio próprio + bug de SEO (01/07 tarde):** `www.garagemrucula.com.br` no ar. A env var `NEXT_PUBLIC_SITE_URL` tinha vazado `http://localhost:3000` do `.env.local` pra Vercel → como `NEXT_PUBLIC_*` é embutida no **build**, sitemap/robots/canonical/og/QR saíam com `localhost` em produção. `siteUrl()` (`src/lib/site.ts`) virou **defensivo** (env não-local vence; senão em deploy Vercel usa o domínio; no client QR/share usam `window.location.origin`); Felipe também corrigiu a env na Vercel. Provado com `VERCEL=1` + env localhost → gera o domínio. **Lição:** nunca copiar `.env.local` inteiro pra Vercel sem revisar `NEXT_PUBLIC_SITE_URL`.
- **✨ Leva de melhorias (commit `4570aa2`, 01/07 tarde):** **fotos HD** (AVIF/WebP + `images.qualities`/`formats` no `next.config`; galeria `quality=90`, cards 82, thumbs do admin nítidos — antes `sizes=120px` borrado; original vai intacto pro Storage, era só exibição); **galeria em TELA CHEIA** (`Lightbox.tsx` — toca na foto → fullscreen com swipe/setas/teclado/contador/trava-scroll; montado na `CarGallery`, botão "ampliar" só em foto); botões **Apagar/Duplicar** do admin com destaque (borda/fundo, antes texto apagado); **filtros da vitrine COLAPSÁVEIS no mobile** (busca sempre visível + botão "Filtros" com `panelCount`); **safe-area do iPhone** no WhatsApp flutuante e voltar-ao-topo (`bottom-[calc(...+env(safe-area-inset-bottom))]`); menu mobile fecha em route change; **rate-limit** in-memory no `/api/lead` (30/min por IP, `src/lib/rate-limit.ts`) + validação UUID/sanitização (nunca bloqueia o WhatsApp, que é `<a>` + fetch keepalive). **QA por screenshot** (dev-browser `--headless`) em mobile(390)/tablet(820)/desktop(1440) — nada quebrando.
- **🎨 Camada cosmética "UAU" (commit `e8269ab`):** **reveal-on-scroll** (IntersectionObserver + cascata/stagger) com **js-gate à prova de falha** — script inline no `layout.tsx` marca `.js-reveal` no `<html>` ANTES da 1ª pintura e o CSS só esconde `[data-reveal]` quando essa classe existe (sem JS ou com reduced-motion nada some; `<html suppressHydrationWarning>`). **Barra de progresso** no topo (`ScrollProgress.tsx`, via rAF). **Spotlight** radial seguindo o cursor nos cards (`Reveal.tsx` = pointermove global + IntersectionObserver que re-observa por `usePathname`; classe `.spotlight` + `z-[2]` no texto do `CarCard`). **Hero +impactante:** grade `.hero-grid`, 3º orb amarelo, orbs flutuando (`orb-float-a/b/c`), glow `.text-glow-rucula`. **Microinterações:** shine varrendo botões cheios, hover glow no WhatsApp/voltar-ao-topo, **glass** (backdrop-blur) no header/filtros/banner; filtros da vitrine **sticky** no desktop. Regras em `globals.css`; componentes novos `Reveal.tsx` + `ScrollProgress.tsx` montados no `(site)/layout.tsx`. Tudo respeita `prefers-reduced-motion`. ✅ **VALIDADO por screenshot** (mobile/tablet/desktop) em 01/07 — o dev-browser funcionou com `dev-browser --headless run`.
- **Site público (v1):** home (hero c/ orbs + destaques + grid), vitrine c/ filtros (marca/ano/preço/estilo/busca + limpar), página do carro (galeria fotos+vídeos, ficha c/ ícones, "tenho interesse"→WhatsApp **e registra lead**, QR, compartilhar, "veja também", selo VENDIDO), sobre (c/ diferenciais), 404, error.tsx.
- **Admin** (`/admin`, protegido por `proxy.ts` + e-mails em `lib/admins`): login **e-mail+senha OU Google (OAuth)**, dashboard (4 métricas + leads recentes), CRUD de carros (upload fotos c/ arrastar @dnd-kit + vídeos, ficha completa, tags, mods), status/destaque, **duplicar carro**, **histórico de vendas** (tabela `car_sales`, privado), leads, **trocar senha** (/admin/senha), **toast** de confirmação. Header responsivo (mobile ok).
- **Infra:** PWA (SW network-first) + banner "instalar app"; SEO (sitemap/robots/JSON-LD); favicon; Vercel Analytics. Supabase (clients read/server/admin, RLS, bucket `car-media` público, RPC `bump_car_view`).
- **Senha do admin:** trocada pelo Felipe — **NUNCA versionar senha** (repo é público).

**Decisões fechadas:** sem vídeo na home; tema escuro/preto; vendidos c/ selo "VENDIDO"; **marca no site/admin SEM acento ("Rucula", 02/07)** — só os `.md` internos ficam com "Rúcula"; extras v1 (QR, leads, views, histórico vendas) TODOS feitos. Paleta: verde rúcula #24a54b (claro #4ade80) + petróleo #013c43 + amarelo Senna #f3e838 + azul #204ba0 sobre preto #0a0a0a.

**Contas:** Supabase = `garagemrucula@gmail.com` (conta nova; ref projeto `lryzyydzjodywvzhiumx`). Vercel = `felipeherrera.contato@gmail.com` (Hobby). git `user.email` DEVE ser `felipeherrera.contato@gmail.com` (bate com a Vercel).

---

## Índice
- [Projeto — plano/escopo completo](project_garagem_rucula.md)
- [Setup & continuidade (local + GitHub)](setup_continuidade.md)
- [Aprendizados técnicos (Rebobina + deploy/infra desta sessão)](aprendizados_rebobina.md)
